---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="ko">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>한식의 정석</h1>
				<p class="subtitle">정갈한 한 그릇, 깊은 한 끼의 이야기</p>
				<hr />
				<p>
					오래도록 사랑받아 온 한식의 맛과 멋을 기록합니다.<br />
					집에서도 쉽게 따라 할 수 있는 레시피부터,<br />
					한식에 담긴 이야기까지.
				</p>
				<a href="/blog" class="cta">글 보러 가기 &rarr;</a>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.hero {
		text-align: center;
		padding: 4em 0;
	}
	.hero h1 {
		font-size: 2.8em;
		letter-spacing: -0.03em;
		margin-bottom: 0.3em;
	}
	.subtitle {
		color: rgb(var(--gray));
		font-size: 1.1em;
		margin-bottom: 1.5em;
	}
	.hero p {
		line-height: 2;
		color: rgb(var(--gray-dark));
	}
	.cta {
		display: inline-block;
		margin-top: 2em;
		padding: 0.7em 2em;
		border: 1px solid rgb(var(--black));
		color: rgb(var(--black));
		text-decoration: none;
		font-size: 0.95em;
		transition: all 0.2s ease;
	}
	.cta:hover {
		background: rgb(var(--black));
		color: #ffffff;
	}
</style>
